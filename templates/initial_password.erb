The initial password for the user account, '<%= @user_acct %>', that has
been created to administer the CollectionSpace system on this host, is:

<%= @password %>

You can change this password to another password of your choosing.

To do so (on a Linux, OS X, or other Unix-like system), enter the following
command at your command prompt:

passwd

After changing your password, and recording your new password in a
secure place, you can then safely delete this text file.